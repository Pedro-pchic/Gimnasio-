@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-2xl space-y-6"><h1 class="text-2xl font-semibold">Nuevo certificado</h1><form method="POST" action="{{ route('quality-certificates.store') }}" class="space-y-4 rounded-lg bg-white p-6 shadow ring-1 ring-gray-200">@csrf<select name="purchase_order_id" required class="w-full rounded border-gray-300"><option value="">Orden de compra</option>@foreach($purchaseOrders as $order)<option value="{{ $order->id }}">#{{ $order->id }} — {{ $order->supplier->name }}</option>@endforeach</select><input name="number" required placeholder="Número" class="w-full rounded border-gray-300"><div class="grid grid-cols-2 gap-4"><input type="date" name="issued_at" required class="rounded border-gray-300"><input type="date" name="expires_at" class="rounded border-gray-300"></div><select name="status" class="w-full rounded border-gray-300"><option value="pending">Pendiente</option><option value="valid">Válido</option><option value="expired">Vencido</option></select><textarea name="notes" placeholder="Notas" class="w-full rounded border-gray-300"></textarea><button class="rounded bg-indigo-600 px-4 py-2 text-white">Guardar</button></form></div>
@endsection
