@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-2xl space-y-6"><h1 class="text-2xl font-semibold">{{ $supplier->exists ? 'Editar proveedor' : 'Nuevo proveedor' }}</h1><form method="POST" action="{{ $supplier->exists ? route('suppliers.update', $supplier) : route('suppliers.store') }}" class="space-y-4 rounded-lg bg-white p-6 shadow ring-1 ring-gray-200">@csrf @if($supplier->exists) @method('PUT') @endif
<x-field-error field="name" /><input name="name" value="{{ old('name', $supplier->name) }}" required placeholder="Nombre" class="w-full rounded border-gray-300">
<input name="contact" value="{{ old('contact', $supplier->contact) }}" placeholder="Contacto" class="w-full rounded border-gray-300"><input name="phone" value="{{ old('phone', $supplier->phone) }}" placeholder="Teléfono" class="w-full rounded border-gray-300"><input type="email" name="email" value="{{ old('email', $supplier->email) }}" placeholder="Correo" class="w-full rounded border-gray-300"><textarea name="notes" placeholder="Notas" class="w-full rounded border-gray-300">{{ old('notes', $supplier->notes) }}</textarea><label class="flex gap-2"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $supplier->exists ? $supplier->is_active : true))> Activo</label><button class="rounded bg-indigo-600 px-4 py-2 text-white">Guardar</button></form></div>
@endsection
