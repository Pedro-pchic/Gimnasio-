@extends('layouts.admin')

@section('title', 'Descuentos')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Catalogo externo</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Descuentos comerciales</h1><p class="mt-2 text-slate-600">Reglas aplicadas automaticamente a los articulos externos disponibles.</p></div>@can('discounts.manage')<a href="{{ route('discounts.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nuevo descuento</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Descuento</th><th class="px-5 py-3">Valor</th><th class="px-5 py-3">Vigencia</th><th class="px-5 py-3">Articulos</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($discounts as $discount)<tr><td class="px-5 py-4"><a href="{{ route('discounts.show', $discount) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $discount->name }}</a><p class="mt-1 text-xs text-slate-500">{{ $discount->branches->pluck('name')->join(', ') ?: 'Todas las sucursales' }}</p></td><td class="px-5 py-4 text-slate-600">{{ number_format((float) $discount->value, 2) }}{{ $discount->type->value === 'percentage' ? '%' : '' }}</td><td class="px-5 py-4 text-slate-600">{{ $discount->start_date?->format('Y-m-d') ?: 'Sin inicio' }}<br>{{ $discount->end_date?->format('Y-m-d') ?: 'Sin fin' }}</td><td class="px-5 py-4 text-slate-600">{{ $discount->thirdPartyItems->pluck('name')->join(', ') }}</td><td class="px-5 py-4"><x-status-badge :active="$discount->is_active" /></td><td class="px-5 py-4 text-right"><a href="{{ route('discounts.show', $discount) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No hay descuentos registrados.</td></tr>@endforelse</tbody></table></div>@if ($discounts->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $discounts->links() }}</div>@endif</div>
@endsection
