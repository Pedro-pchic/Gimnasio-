@extends('layouts.admin')

@section('title', $service ? 'Editar servicio' : 'Nuevo servicio')

@section('content')
    <div class="mb-6"><a href="{{ route('services.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">← Volver a servicios</a><h1 class="mt-3 text-3xl font-bold tracking-tight">{{ $service ? 'Editar servicio' : 'Nuevo servicio' }}</h1></div>
    <form method="POST" action="{{ $service ? route('services.update', $service) : route('services.store') }}" class="max-w-3xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7">@csrf @if ($service) @method('PUT') @endif<div class="grid gap-5"><div><label for="name" class="text-sm font-medium text-slate-700">Nombre</label><input id="name" name="name" value="{{ old('name', $service?->name) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="name" /></div><div><label for="description" class="text-sm font-medium text-slate-700">Descripción</label><textarea id="description" name="description" rows="4" class="mt-1 block w-full rounded-lg border-slate-300">{{ old('description', $service?->description) }}</textarea><x-field-error name="description" /></div><label class="flex items-center gap-2 text-sm text-slate-700"><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $service?->is_active ?? true)) class="rounded border-slate-300 text-indigo-600">Servicio activo</label></div><div class="mt-7 flex flex-wrap gap-3"><button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">{{ $service ? 'Guardar cambios' : 'Crear servicio' }}</button><a href="{{ $service ? route('services.show', $service) : route('services.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</a></div></form>
@endsection
