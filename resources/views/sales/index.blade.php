@extends('layouts.admin')

@section('title', 'Ventas')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Finanzas</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Ventas</h1><p class="mt-2 text-slate-600">Ventas internas con detalle, pago y comprobante no fiscal.</p></div>@can('sales.manage')<a href="{{ route('sales.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Registrar venta</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Fecha</th><th class="px-5 py-3">Cliente</th><th class="px-5 py-3">Sucursal</th><th class="px-5 py-3">Total</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($sales as $sale)<tr><td class="px-5 py-4 text-slate-600">{{ $sale->sale_date->format('d/m/Y') }}</td><td class="px-5 py-4">@if ($sale->client)<a href="{{ route('clients.show', $sale->client) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $sale->client->first_name }} {{ $sale->client->last_name }}</a>@else<span class="text-slate-500">Sin cliente</span>@endif</td><td class="px-5 py-4 text-slate-600">{{ $sale->branch->name }}</td><td class="px-5 py-4 font-medium text-slate-800">{{ number_format((float) $sale->total, 2) }}</td><td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700">{{ ucfirst($sale->status->value) }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('sales.show', $sale) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No hay ventas registradas.</td></tr>@endforelse</tbody></table></div>@if ($sales->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $sales->links() }}</div>@endif</div>
@endsection
