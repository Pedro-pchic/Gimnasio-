@extends('layouts.admin')

@section('title', 'Tipos de membresía')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Catálogo</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Tipos de membresía</h1><p class="mt-2 text-slate-600">Configura los planes y sus beneficios asociados.</p></div>@can('membership-types.manage')<a href="{{ route('membership-types.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nuevo tipo</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Membresía</th><th class="px-5 py-3">Precio de referencia</th><th class="px-5 py-3">Beneficios</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($membershipTypes as $membershipType)<tr><td class="px-5 py-4"><a href="{{ route('membership-types.show', $membershipType) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $membershipType->name }}</a><p class="mt-1 text-xs text-slate-500">{{ $membershipType->description ?: 'Sin descripción' }}</p></td><td class="px-5 py-4 text-slate-600">{{ number_format((float) $membershipType->reference_price, 2) }}</td><td class="px-5 py-4 text-slate-600">{{ $membershipType->benefits->pluck('name')->join(', ') ?: 'Sin beneficios' }}</td><td class="px-5 py-4"><x-status-badge :active="$membershipType->is_active" /></td><td class="px-5 py-4 text-right"><a href="{{ route('membership-types.show', $membershipType) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No hay tipos de membresía registrados.</td></tr>@endforelse</tbody></table></div>@if ($membershipTypes->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $membershipTypes->links() }}</div>@endif</div>
@endsection
