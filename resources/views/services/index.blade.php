@extends('layouts.admin')

@section('title', 'Servicios')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Catálogo</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Servicios</h1><p class="mt-2 text-slate-600">Servicios que pueden asociarse a una sucursal.</p></div>@can('services.manage')<a href="{{ route('services.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nuevo servicio</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Servicio</th><th class="px-5 py-3">Sucursales</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($services as $service)<tr><td class="px-5 py-4"><a href="{{ route('services.show', $service) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $service->name }}</a><p class="mt-1 text-xs text-slate-500">{{ $service->description ?: 'Sin descripción' }}</p></td><td class="px-5 py-4 text-slate-600">{{ $service->branches->pluck('name')->join(', ') ?: 'Sin sucursales' }}</td><td class="px-5 py-4"><x-status-badge :active="$service->is_active" /></td><td class="px-5 py-4 text-right"><a href="{{ route('services.show', $service) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="4" class="px-5 py-10 text-center text-slate-500">No hay servicios registrados.</td></tr>@endforelse</tbody></table></div>@if ($services->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $services->links() }}</div>@endif</div>
@endsection
