@extends('layouts.admin')

@section('title', 'Registrar referido')

@section('content')
    <div class="mb-6"><a href="{{ route('referrals.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Volver a referidos</a><h1 class="mt-3 text-3xl font-bold tracking-tight">Registrar referido</h1><p class="mt-2 text-slate-600">El beneficio no se entrega en este paso.</p></div>
    <form method="POST" action="{{ route('referrals.store') }}" class="max-w-3xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7">@csrf<div class="grid gap-5 sm:grid-cols-2"><div><label for="referrer_client_id" class="text-sm font-medium text-slate-700">Cliente que refiere</label><select id="referrer_client_id" name="referrer_client_id" required class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Selecciona un cliente</option>@foreach ($clients as $client)<option value="{{ $client->id }}" @selected((int) old('referrer_client_id') === $client->id)>{{ $client->last_name }}, {{ $client->first_name }} · {{ $client->code }}</option>@endforeach</select><x-field-error name="referrer_client_id" /></div><div><label for="referred_client_id" class="text-sm font-medium text-slate-700">Cliente referido</label><select id="referred_client_id" name="referred_client_id" required class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Selecciona un cliente</option>@foreach ($clients as $client)<option value="{{ $client->id }}" @selected((int) old('referred_client_id') === $client->id)>{{ $client->last_name }}, {{ $client->first_name }} · {{ $client->code }}</option>@endforeach</select><x-field-error name="referred_client_id" /></div></div><div class="mt-7 flex flex-wrap gap-3"><button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">Registrar referido</button><a href="{{ route('referrals.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</a></div></form>
@endsection
