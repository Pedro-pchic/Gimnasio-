@extends('layouts.admin')
@section('content')
<div class="space-y-6"><div class="flex justify-between"><h1 class="text-2xl font-semibold">Orden #{{ $purchaseOrder->id }}</h1>@if($purchaseOrder->status->value === 'pending')<form method="POST" action="{{ route('purchase-orders.receive', $purchaseOrder) }}">@csrf @method('PATCH')<button class="rounded bg-emerald-600 px-4 py-2 text-white">Recibir orden</button></form>@endif</div><p>Proveedor: {{ $purchaseOrder->supplier->name }} · Estado: {{ $purchaseOrder->status->label() }} · Total: {{ number_format((float) $purchaseOrder->total, 2) }}</p><div class="rounded-lg bg-white p-4 shadow ring-1 ring-gray-200"><table class="min-w-full text-sm"><thead><tr><th class="text-left">Artículo</th><th class="text-left">Cantidad</th><th class="text-left">Subtotal</th></tr></thead><tbody>@foreach($purchaseOrder->items as $item)<tr><td>{{ $item->inventoryItem->name }}</td><td>{{ $item->quantity }}</td><td>{{ number_format((float) $item->subtotal, 2) }}</td></tr>@endforeach</tbody></table></div><a class="text-indigo-600" href="{{ route('quality-certificates.create') }}">Registrar certificado de calidad</a></div>
@endsection
