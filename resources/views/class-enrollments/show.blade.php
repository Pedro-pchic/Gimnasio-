@extends('layouts.admin')

@section('title', 'Inscripción #'.$enrollment->id)

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><a href="{{ route('class-enrollments.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">← Volver a participantes</a><h1 class="mt-3 text-3xl font-bold tracking-tight">Inscripción #{{ $enrollment->id }}</h1><p class="mt-2 text-slate-600">{{ $enrollment->gymClassSchedule->gymClass->name }} · {{ $enrollment->enrollment_date->format('d/m/Y') }}</p></div><div class="flex flex-wrap gap-2">@can('enrollments.attendance')@if ($enrollment->status !== \App\GymClassEnrollmentStatus::Cancelled)<form method="POST" action="{{ route('class-enrollments.attendance', $enrollment) }}">@csrf @method('PATCH')<button class="rounded-lg border border-emerald-600 px-4 py-2.5 text-sm font-semibold text-emerald-700 hover:bg-emerald-50">Marcar asistencia</button></form>@endif@endcan @can('enrollments.register')@if ($enrollment->status !== \App\GymClassEnrollmentStatus::Cancelled)<form method="POST" action="{{ route('class-enrollments.cancel', $enrollment) }}" onsubmit="return confirm('¿Deseas cancelar esta inscripción?');">@csrf @method('PATCH')<button class="rounded-lg border border-red-300 px-4 py-2.5 text-sm font-semibold text-red-700 hover:bg-red-50">Cancelar inscripción</button></form>@endif@endcan</div></div>
    <section class="max-w-3xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7"><dl class="grid gap-5 sm:grid-cols-2"><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Cliente</dt><dd class="mt-1"><a href="{{ route('clients.show', $enrollment->client) }}" class="font-medium text-indigo-700 hover:text-indigo-500">{{ $enrollment->client->first_name }} {{ $enrollment->client->last_name }}</a></dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Estado</dt><dd class="mt-1 text-slate-800">{{ ucfirst($enrollment->status->value) }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Actividad</dt><dd class="mt-1 text-slate-800">{{ $enrollment->gymClassSchedule->gymClass->name }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Sucursal</dt><dd class="mt-1 text-slate-800">{{ $enrollment->gymClassSchedule->gymClass->branch->name }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Fecha</dt><dd class="mt-1 text-slate-800">{{ $enrollment->enrollment_date->format('d/m/Y') }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Horario</dt><dd class="mt-1 text-slate-800">{{ ucfirst($enrollment->gymClassSchedule->day_of_week) }} · {{ substr($enrollment->gymClassSchedule->start_time, 0, 5) }} – {{ substr($enrollment->gymClassSchedule->end_time, 0, 5) }}</dd></div></dl></section>
@endsection
