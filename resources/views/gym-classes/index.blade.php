@extends('layouts.admin')

@section('title', 'Clases')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Actividades</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Clases</h1><p class="mt-2 text-slate-600">Administra actividades configurables por sucursal y su capacidad máxima.</p></div>@can('classes.manage')<a href="{{ route('gym-classes.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nueva clase</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Actividad</th><th class="px-5 py-3">Tipo</th><th class="px-5 py-3">Sucursal</th><th class="px-5 py-3">Capacidad</th><th class="px-5 py-3">Horarios</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($gymClasses as $gymClass)<tr><td class="px-5 py-4"><a href="{{ route('gym-classes.show', $gymClass) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $gymClass->name }}</a><p class="mt-1 text-xs text-slate-500">{{ $gymClass->description ?: 'Sin descripción' }}</p></td><td class="px-5 py-4 text-slate-600">{{ ucfirst($gymClass->type) }}</td><td class="px-5 py-4 text-slate-600">{{ $gymClass->branch->name }}</td><td class="px-5 py-4 text-slate-600">{{ $gymClass->maximum_capacity }}</td><td class="px-5 py-4 text-slate-600">{{ $gymClass->schedules_count }}</td><td class="px-5 py-4"><x-status-badge :active="$gymClass->is_active" /></td><td class="px-5 py-4 text-right"><a href="{{ route('gym-classes.show', $gymClass) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="7" class="px-5 py-10 text-center text-slate-500">No hay clases registradas.</td></tr>@endforelse</tbody></table></div>@if ($gymClasses->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $gymClasses->links() }}</div>@endif</div>
@endsection
