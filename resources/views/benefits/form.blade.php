@extends('layouts.admin')

@section('title', $benefit ? 'Editar beneficio' : 'Nuevo beneficio')

@section('content')
    @php($selectedMembershipTypeIds = old('membership_type_ids', $benefit?->membershipTypes->modelKeys() ?? []))
    <div class="mb-6"><a href="{{ route('benefits.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">← Volver a beneficios</a><h1 class="mt-3 text-3xl font-bold tracking-tight">{{ $benefit ? 'Editar beneficio' : 'Nuevo beneficio' }}</h1></div>
    <form method="POST" action="{{ $benefit ? route('benefits.update', $benefit) : route('benefits.store') }}" class="max-w-4xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7">@csrf @if ($benefit) @method('PUT') @endif<div class="grid gap-5 sm:grid-cols-2"><div><label for="name" class="text-sm font-medium text-slate-700">Nombre</label><input id="name" name="name" value="{{ old('name', $benefit?->name) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="name" /></div><div><label for="benefit_type" class="text-sm font-medium text-slate-700">Tipo</label><input id="benefit_type" name="benefit_type" value="{{ old('benefit_type', $benefit?->benefit_type) }}" placeholder="Ej. descuento, uso" class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="benefit_type" /></div><div><label for="value" class="text-sm font-medium text-slate-700">Valor</label><input id="value" name="value" type="number" min="0" step="0.01" value="{{ old('value', $benefit?->value) }}" class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="value" /></div><div><label for="usage_limit" class="text-sm font-medium text-slate-700">Límite de uso</label><input id="usage_limit" name="usage_limit" type="number" min="1" value="{{ old('usage_limit', $benefit?->usage_limit) }}" class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="usage_limit" /></div><div class="sm:col-span-2"><label for="usage_period" class="text-sm font-medium text-slate-700">Período de uso</label><input id="usage_period" name="usage_period" value="{{ old('usage_period', $benefit?->usage_period) }}" placeholder="Ej. mensual" class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="usage_period" /></div><div class="sm:col-span-2"><label for="description" class="text-sm font-medium text-slate-700">Descripción</label><textarea id="description" name="description" rows="4" class="mt-1 block w-full rounded-lg border-slate-300">{{ old('description', $benefit?->description) }}</textarea><x-field-error name="description" /></div><fieldset class="sm:col-span-2"><legend class="text-sm font-medium text-slate-700">Tipos de membresía asociados</legend><div class="mt-2 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">@foreach ($membershipTypes as $membershipType)<label class="flex items-center gap-2 rounded-lg border border-slate-200 p-3 text-sm"><input type="checkbox" name="membership_type_ids[]" value="{{ $membershipType->id }}" @checked(in_array($membershipType->id, array_map('intval', $selectedMembershipTypeIds), true)) class="rounded border-slate-300 text-indigo-600">{{ $membershipType->name }}</label>@endforeach</div><x-field-error name="membership_type_ids" /></fieldset><label class="flex items-center gap-2 text-sm text-slate-700 sm:col-span-2"><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $benefit?->is_active ?? true)) class="rounded border-slate-300 text-indigo-600">Beneficio activo</label></div><div class="mt-7 flex flex-wrap gap-3"><button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">{{ $benefit ? 'Guardar cambios' : 'Crear beneficio' }}</button><a href="{{ $benefit ? route('benefits.show', $benefit) : route('benefits.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</a></div></form>
@endsection
