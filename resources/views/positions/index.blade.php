@extends('layouts.admin')

@section('title', 'Puestos')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Personal</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Puestos</h1><p class="mt-2 text-slate-600">Puestos configurables para el personal del gimnasio.</p></div><a href="{{ route('positions.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nuevo puesto</a></div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Puesto</th><th class="px-5 py-3">Descripcion</th><th class="px-5 py-3">Puede impartir clases</th><th class="px-5 py-3">Empleados</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($positions as $position)<tr><td class="px-5 py-4 font-semibold text-slate-800">{{ $position->name }}</td><td class="px-5 py-4 text-slate-600">{{ $position->description ?: 'Sin descripcion' }}</td><td class="px-5 py-4 text-slate-600">{{ $position->can_teach ? 'Si' : 'No' }}</td><td class="px-5 py-4 text-slate-600">{{ $position->employees_count }}</td><td class="px-5 py-4"><x-status-badge :active="$position->is_active" /></td><td class="px-5 py-4 text-right"><div class="inline-flex items-center gap-3"><a href="{{ route('positions.edit', $position) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Editar</a><form method="POST" action="{{ route('positions.toggle-status', $position) }}">@csrf @method('PATCH')<button class="text-sm font-medium text-slate-700 hover:text-slate-500">{{ $position->is_active ? 'Desactivar' : 'Activar' }}</button></form></div></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No hay puestos configurados.</td></tr>@endforelse</tbody></table></div>@if ($positions->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $positions->links() }}</div>@endif</div>
@endsection
