@extends('layouts.admin')

@section('title', $sale->receipt?->number ?? 'Comprobante')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><a href="{{ route('sales.show', $sale) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">← Volver a la venta</a><p class="mt-3 text-sm font-semibold uppercase tracking-widest text-indigo-600">Comprobante interno</p><h1 class="mt-1 text-3xl font-bold tracking-tight">{{ $sale->receipt->number }}</h1><p class="mt-2 text-slate-600">Este documento no es factura electrónica ni documento fiscal.</p></div><button type="button" onclick="window.print()" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Imprimir</button></div>
    <article class="max-w-3xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-8"><div class="flex flex-col justify-between gap-4 border-b border-slate-200 pb-6 sm:flex-row"><div><h2 class="text-xl font-bold">Gimnasio · Administración</h2><p class="mt-1 text-sm text-slate-600">Comprobante de pago interno</p></div><dl class="text-sm text-slate-600"><div><dt class="inline font-semibold text-slate-800">Fecha:</dt><dd class="inline"> {{ $sale->receipt->issued_at->format('d/m/Y') }}</dd></div><div><dt class="inline font-semibold text-slate-800">Método:</dt><dd class="inline"> {{ ucfirst($sale->receipt->payment_method->value) }}</dd></div></dl></div><dl class="mt-6 grid gap-4 sm:grid-cols-2"><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Cliente</dt><dd class="mt-1 text-slate-800">{{ $sale->client ? $sale->client->first_name.' '.$sale->client->last_name : 'Venta sin cliente' }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Sucursal</dt><dd class="mt-1 text-slate-800">{{ $sale->branch->name }}</dd></div></dl><table class="mt-6 min-w-full divide-y divide-slate-200 text-sm"><thead class="text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="py-3">Descripción</th><th class="py-3 text-right">Cantidad</th><th class="py-3 text-right">Importe</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($sale->details as $detail)<tr><td class="py-3 text-slate-800">{{ $detail->description }}</td><td class="py-3 text-right text-slate-600">{{ number_format((float) $detail->quantity, 2) }}</td><td class="py-3 text-right text-slate-800">{{ number_format((float) $detail->subtotal, 2) }}</td></tr>@endforeach</tbody></table><dl class="ml-auto mt-6 max-w-xs space-y-2 text-sm"><div class="flex justify-between gap-4"><dt class="text-slate-600">Subtotal</dt><dd class="text-slate-800">{{ number_format((float) $sale->subtotal, 2) }}</dd></div><div class="flex justify-between gap-4"><dt class="text-slate-600">Descuento</dt><dd class="text-slate-800">{{ number_format((float) $sale->discount, 2) }}</dd></div><div class="flex justify-between gap-4 border-t border-slate-200 pt-3 text-base font-bold"><dt>Total</dt><dd>{{ number_format((float) $sale->total, 2) }}</dd></div></dl></article>
@endsection
