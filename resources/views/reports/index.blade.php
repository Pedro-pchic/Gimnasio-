@extends('layouts.admin')
@section('content')
<div class="space-y-6"><h1 class="text-2xl font-semibold">Reportes operativos</h1><form method="GET" class="flex flex-wrap items-end gap-3 rounded-lg bg-white p-4 shadow ring-1 ring-gray-200"><label>Desde<input type="date" name="from" value="{{ $filters['from'] }}" class="ml-2 rounded border-gray-300"></label><label>Hasta<input type="date" name="to" value="{{ $filters['to'] }}" class="ml-2 rounded border-gray-300"></label><label>Sucursal<select name="branch_id" class="ml-2 rounded border-gray-300"><option value="">Todas</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected($filters['branch_id'] == $branch->id)>{{ $branch->name }}</option>@endforeach</select></label><button class="rounded bg-indigo-600 px-4 py-2 text-white">Filtrar</button></form><div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach($metrics as $metric)<div class="rounded-lg bg-white p-4 shadow ring-1 ring-gray-200"><p class="text-sm text-gray-500">{{ $metric['label'] }}</p><p class="mt-2 text-2xl font-semibold">{{ is_numeric($metric['value']) ? number_format((float) $metric['value'], 2) : $metric['value'] }}</p></div>@endforeach</div></div>
@endsection
