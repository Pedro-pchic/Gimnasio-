@extends('layouts.admin')
@section('content')
<div class="space-y-6"><div class="flex justify-between"><h1 class="text-2xl font-semibold">Bonos de empleados</h1><a href="{{ route('employee-bonuses.create') }}" class="rounded bg-indigo-600 px-4 py-2 text-white">Registrar bono</a></div><div class="rounded-lg bg-white p-4 shadow ring-1 ring-gray-200"><table class="min-w-full text-sm"><thead><tr><th class="text-left">Empleado</th><th class="text-left">Periodo</th><th class="text-left">Monto</th><th class="text-left">Estado</th><th></th></tr></thead><tbody>@forelse($bonuses as $bonus)<tr><td>{{ $bonus->employee->fullName() }}</td><td>{{ $bonus->period }}</td><td>{{ number_format((float) $bonus->amount, 2) }}</td><td>{{ $bonus->status->label() }}</td><td class="space-x-2">@if($bonus->status->value === 'pending')<form class="inline" method="POST" action="{{ route('employee-bonuses.approve', $bonus) }}">@csrf @method('PATCH')<button class="text-emerald-600">Aprobar</button></form><form class="inline" method="POST" action="{{ route('employee-bonuses.cancel', $bonus) }}">@csrf @method('PATCH')<button class="text-red-600">Cancelar</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="py-6 text-center text-gray-500">Sin bonos.</td></tr>@endforelse</tbody></table></div></div>
@endsection
