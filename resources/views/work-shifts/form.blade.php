@extends('layouts.admin')

@section('title', $workShift ? 'Editar turno' : 'Nuevo turno')

@section('content')
    <div class="mb-6"><a href="{{ route('work-shifts.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Volver a turnos</a><h1 class="mt-3 text-3xl font-bold tracking-tight">{{ $workShift ? 'Editar turno' : 'Nuevo turno' }}</h1></div>
    <form method="POST" action="{{ $workShift ? route('work-shifts.update', $workShift) : route('work-shifts.store') }}" class="max-w-3xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7">@csrf @if ($workShift) @method('PUT') @endif<div class="grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><label for="name" class="text-sm font-medium text-slate-700">Nombre</label><input id="name" name="name" value="{{ old('name', $workShift?->name) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="name" /></div><div><label for="start_time" class="text-sm font-medium text-slate-700">Hora de inicio</label><input id="start_time" name="start_time" type="time" value="{{ old('start_time', $workShift ? substr($workShift->start_time, 0, 5) : '08:00') }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="start_time" /></div><div><label for="end_time" class="text-sm font-medium text-slate-700">Hora de fin</label><input id="end_time" name="end_time" type="time" value="{{ old('end_time', $workShift ? substr($workShift->end_time, 0, 5) : '17:00') }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="end_time" /></div><p class="text-xs text-slate-500 sm:col-span-2">Si la hora final es anterior a la inicial, el turno termina al dia siguiente.</p><label class="flex items-center gap-2 text-sm font-medium text-slate-700 sm:col-span-2"><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $workShift?->is_active ?? true)) class="rounded border-slate-300 text-indigo-600"> Turno activo</label></div><div class="mt-7 flex flex-wrap gap-3"><button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">{{ $workShift ? 'Guardar cambios' : 'Crear turno' }}</button><a href="{{ route('work-shifts.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</a></div></form>
@endsection
