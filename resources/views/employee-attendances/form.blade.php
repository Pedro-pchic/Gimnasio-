@extends('layouts.admin')

@section('title', 'Registrar entrada laboral')

@section('content')
    <div class="mb-6"><a href="{{ route('employee-attendances.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Volver al historial</a><h1 class="mt-3 text-3xl font-bold tracking-tight">Registrar entrada laboral</h1><p class="mt-2 text-slate-600">El turno aplicable se asigna automaticamente al guardar la entrada.</p></div>
    <form method="POST" action="{{ route('employee-attendances.store') }}" class="max-w-3xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7">@csrf<div class="grid gap-5"><div><label for="employee_id" class="text-sm font-medium text-slate-700">Empleado</label><select id="employee_id" name="employee_id" required class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Selecciona un empleado activo</option>@foreach ($employees as $employee)@php($shift = $employee->shiftAssignments->first()?->workShift)<option value="{{ $employee->id }}" @selected((int) old('employee_id', $selectedEmployeeId) === $employee->id)>{{ $employee->fullName() }} · {{ $employee->position->name }} · {{ $employee->branch->name }} · {{ $shift ? $shift->name : 'Sin turno' }}</option>@endforeach</select><x-field-error name="employee_id" /></div><div class="grid gap-5 sm:grid-cols-2"><div><label for="attendance_date" class="text-sm font-medium text-slate-700">Fecha</label><input id="attendance_date" name="attendance_date" type="date" value="{{ old('attendance_date', $attendanceDate) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="attendance_date" /></div><div><label for="entry_time" class="text-sm font-medium text-slate-700">Hora de entrada</label><input id="entry_time" name="entry_time" type="time" value="{{ old('entry_time', now()->format('H:i')) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="entry_time" /></div></div><div><label for="observations" class="text-sm font-medium text-slate-700">Observaciones</label><textarea id="observations" name="observations" rows="4" class="mt-1 block w-full rounded-lg border-slate-300">{{ old('observations') }}</textarea><x-field-error name="observations" /></div></div><div class="mt-7 flex flex-wrap gap-3"><button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">Registrar entrada</button><a href="{{ route('employee-attendances.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</a></div></form>
@endsection
