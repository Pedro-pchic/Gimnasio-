@extends('layouts.admin')

@section('title', 'Productos externos')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Catalogo externo</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Productos y servicios externos</h1><p class="mt-2 text-slate-600">Articulos disponibles por sucursal para su venta.</p></div>@can('third-party-items.manage')<a href="{{ route('third-party-items.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nuevo articulo</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Articulo</th><th class="px-5 py-3">Tercero</th><th class="px-5 py-3">Precio base</th><th class="px-5 py-3">Sucursales</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($thirdPartyItems as $thirdPartyItem)<tr><td class="px-5 py-4"><a href="{{ route('third-party-items.show', $thirdPartyItem) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $thirdPartyItem->name }}</a><p class="mt-1 text-xs text-slate-500">{{ ucfirst($thirdPartyItem->type->value) }} · {{ $thirdPartyItem->description ?: 'Sin descripcion' }}</p></td><td class="px-5 py-4 text-slate-600">{{ $thirdPartyItem->commercialPartner->name }}</td><td class="px-5 py-4 text-slate-600">{{ number_format((float) $thirdPartyItem->base_price, 2) }}</td><td class="px-5 py-4 text-slate-600">{{ $thirdPartyItem->branches->pluck('name')->join(', ') }}</td><td class="px-5 py-4"><x-status-badge :active="$thirdPartyItem->is_active" /></td><td class="px-5 py-4 text-right"><a href="{{ route('third-party-items.show', $thirdPartyItem) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No hay productos o servicios externos registrados.</td></tr>@endforelse</tbody></table></div>@if ($thirdPartyItems->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $thirdPartyItems->links() }}</div>@endif</div>
@endsection
