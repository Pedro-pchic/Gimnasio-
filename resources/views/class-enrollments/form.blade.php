@extends('layouts.admin')

@section('title', 'Inscribir cliente')

@section('content')
    <div class="mb-6"><a href="{{ route('class-enrollments.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">← Volver a participantes</a><h1 class="mt-3 text-3xl font-bold tracking-tight">Inscribir cliente</h1><p class="mt-2 text-slate-600">La inscripción valida la membresía, el horario, la fecha y los cupos disponibles en el servidor.</p></div>
    <form method="POST" action="{{ route('class-enrollments.store') }}" class="max-w-4xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7">@csrf<div class="grid gap-5 sm:grid-cols-2"><div><label for="client_id" class="text-sm font-medium text-slate-700">Cliente</label><select id="client_id" name="client_id" required class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Selecciona un cliente</option>@foreach ($clients as $client)<option value="{{ $client->id }}" @selected((int) old('client_id', request('client_id')) === $client->id)>{{ $client->last_name }}, {{ $client->first_name }} · {{ $client->code }}</option>@endforeach</select><x-field-error name="client_id" /></div><div><label for="gym_class_schedule_id" class="text-sm font-medium text-slate-700">Horario de actividad</label><select id="gym_class_schedule_id" name="gym_class_schedule_id" required class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Selecciona un horario</option>@foreach ($schedules as $schedule)<option value="{{ $schedule->id }}" @selected((int) old('gym_class_schedule_id', request('gym_class_schedule_id')) === $schedule->id)>{{ $schedule->gymClass->name }} · {{ $schedule->gymClass->branch->name }} · {{ ucfirst($schedule->day_of_week) }} {{ substr($schedule->start_time, 0, 5) }}</option>@endforeach</select><x-field-error name="gym_class_schedule_id" /></div><div><label for="enrollment_date" class="text-sm font-medium text-slate-700">Fecha de participación</label><input id="enrollment_date" name="enrollment_date" type="date" value="{{ old('enrollment_date', now()->toDateString()) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="enrollment_date" /></div></div><div class="mt-7 flex flex-wrap gap-3"><button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">Registrar inscripción</button><a href="{{ route('class-enrollments.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</a></div></form>
@endsection
