@extends('layouts.admin')

@section('title', $gymClass ? 'Editar clase' : 'Nueva clase')

@section('content')
    <div class="mb-6"><a href="{{ route('gym-classes.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">← Volver a clases</a><h1 class="mt-3 text-3xl font-bold tracking-tight">{{ $gymClass ? 'Editar clase' : 'Nueva clase' }}</h1><p class="mt-2 text-slate-600">Natación y boxeo se validan contra los servicios habilitados en la sucursal cuando existen.</p></div>
    <form method="POST" action="{{ $gymClass ? route('gym-classes.update', $gymClass) : route('gym-classes.store') }}" class="max-w-4xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7">@csrf @if ($gymClass) @method('PUT') @endif<div class="grid gap-5 sm:grid-cols-2"><div><label for="name" class="text-sm font-medium text-slate-700">Nombre</label><input id="name" name="name" type="text" value="{{ old('name', $gymClass?->name) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="name" /></div><div><label for="type" class="text-sm font-medium text-slate-700">Tipo</label><input id="type" name="type" list="class-types" value="{{ old('type', $gymClass?->type ?? 'general') }}" required class="mt-1 block w-full rounded-lg border-slate-300"><datalist id="class-types"><option value="general"><option value="natacion"><option value="boxeo"></datalist><x-field-error name="type" /></div><div><label for="branch_id" class="text-sm font-medium text-slate-700">Sucursal</label><select id="branch_id" name="branch_id" required class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Selecciona una sucursal</option>@foreach ($branches as $branch)<option value="{{ $branch->id }}" @selected((int) old('branch_id', $gymClass?->branch_id) === $branch->id)>{{ $branch->name }}</option>@endforeach</select><x-field-error name="branch_id" /></div><div><label for="maximum_capacity" class="text-sm font-medium text-slate-700">Capacidad máxima</label><input id="maximum_capacity" name="maximum_capacity" type="number" min="1" value="{{ old('maximum_capacity', $gymClass?->maximum_capacity ?? 20) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="maximum_capacity" /></div><div class="sm:col-span-2"><label for="description" class="text-sm font-medium text-slate-700">Descripción</label><textarea id="description" name="description" rows="4" class="mt-1 block w-full rounded-lg border-slate-300">{{ old('description', $gymClass?->description) }}</textarea><x-field-error name="description" /></div><label class="flex items-center gap-2 text-sm font-medium text-slate-700 sm:col-span-2"><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $gymClass?->is_active ?? true)) class="rounded border-slate-300 text-indigo-600"> Actividad activa</label></div><div class="mt-7 flex flex-wrap gap-3"><button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">{{ $gymClass ? 'Guardar cambios' : 'Crear clase' }}</button><a href="{{ $gymClass ? route('gym-classes.show', $gymClass) : route('gym-classes.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</a></div></form>
@endsection
