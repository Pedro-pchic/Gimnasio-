@extends('layouts.admin')

@section('title', 'Participantes')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Actividades</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Participantes</h1><p class="mt-2 text-slate-600">Inscripciones y asistencia por actividad, horario y fecha.</p></div>@can('enrollments.register')<a href="{{ route('class-enrollments.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Inscribir cliente</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Cliente</th><th class="px-5 py-3">Actividad</th><th class="px-5 py-3">Sucursal</th><th class="px-5 py-3">Fecha y horario</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($enrollments as $enrollment)<tr><td class="px-5 py-4"><a href="{{ route('clients.show', $enrollment->client) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $enrollment->client->first_name }} {{ $enrollment->client->last_name }}</a></td><td class="px-5 py-4"><a href="{{ route('gym-classes.show', $enrollment->gymClassSchedule->gymClass) }}" class="font-medium text-indigo-700 hover:text-indigo-500">{{ $enrollment->gymClassSchedule->gymClass->name }}</a></td><td class="px-5 py-4 text-slate-600">{{ $enrollment->gymClassSchedule->gymClass->branch->name }}</td><td class="px-5 py-4 text-slate-600">{{ $enrollment->enrollment_date->format('d/m/Y') }} · {{ substr($enrollment->gymClassSchedule->start_time, 0, 5) }}</td><td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700">{{ ucfirst($enrollment->status->value) }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('class-enrollments.show', $enrollment) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No hay participantes registrados.</td></tr>@endforelse</tbody></table></div>@if ($enrollments->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $enrollments->links() }}</div>@endif</div>
@endsection
