@extends('layouts.admin')

@section('title', 'Referidos')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Programa comercial</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Referidos</h1><p class="mt-2 text-slate-600">El beneficio Q100 se habilita solo después de validar la membresía pagada del referido.</p></div>@can('referrals.register')<a href="{{ route('referrals.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Registrar referido</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Referidor</th><th class="px-5 py-3">Referido</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3">Beneficio</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($referrals as $referral)<tr><td class="px-5 py-4 text-slate-800">{{ $referral->referrer->first_name }} {{ $referral->referrer->last_name }}</td><td class="px-5 py-4 text-slate-800">{{ $referral->referred->first_name }} {{ $referral->referred->last_name }}</td><td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700">{{ ucfirst($referral->status->value) }}</span></td><td class="px-5 py-4 text-slate-600">Q{{ number_format((float) $referral->reward_amount, 2) }} · {{ $referral->reward_status->value }}<br><span class="text-xs">Usado: Q{{ number_format((float) ($referral->used_reward_amount ?? 0), 2) }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('referrals.show', $referral) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No hay referidos registrados.</td></tr>@endforelse</tbody></table></div>@if ($referrals->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $referrals->links() }}</div>@endif</div>
@endsection
