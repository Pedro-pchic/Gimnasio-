@extends('layouts.admin')

@section('title', $clientMembership ? 'Editar membresía' : 'Asignar membresía')

@section('content')
    <div class="mb-6"><a href="{{ route('client-memberships.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">← Volver a membresías de clientes</a><h1 class="mt-3 text-3xl font-bold tracking-tight">{{ $clientMembership ? 'Editar membresía' : 'Asignar membresía' }}</h1></div>
    <form method="POST" action="{{ $clientMembership ? route('client-memberships.update', $clientMembership) : route('client-memberships.store') }}" class="max-w-4xl rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-7">@csrf @if ($clientMembership) @method('PUT') @endif<div class="grid gap-5 sm:grid-cols-2"><div><label for="client_id" class="text-sm font-medium text-slate-700">Cliente</label><select id="client_id" name="client_id" required class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Selecciona un cliente</option>@foreach ($clients as $client)<option value="{{ $client->id }}" @selected((int) old('client_id', $clientMembership?->client_id ?? request('client_id')) === $client->id)>{{ $client->last_name }}, {{ $client->first_name }} · {{ $client->code }}</option>@endforeach</select><x-field-error name="client_id" /></div><div><label for="membership_type_id" class="text-sm font-medium text-slate-700">Tipo de membresía</label><select id="membership_type_id" name="membership_type_id" required class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Selecciona un tipo</option>@foreach ($membershipTypes as $membershipType)<option value="{{ $membershipType->id }}" @selected((int) old('membership_type_id', $clientMembership?->membership_type_id) === $membershipType->id)>{{ $membershipType->name }}</option>@endforeach</select><x-field-error name="membership_type_id" /></div><div><label for="start_date" class="text-sm font-medium text-slate-700">Fecha de inicio</label><input id="start_date" name="start_date" type="date" value="{{ old('start_date', $clientMembership?->start_date?->toDateString() ?? now()->toDateString()) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="start_date" /></div><div><label for="end_date" class="text-sm font-medium text-slate-700">Fecha de vencimiento</label><input id="end_date" name="end_date" type="date" value="{{ old('end_date', $clientMembership?->end_date?->toDateString()) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="end_date" /></div><div><label for="applied_price" class="text-sm font-medium text-slate-700">Precio aplicado</label><input id="applied_price" name="applied_price" type="number" min="0" step="0.01" value="{{ old('applied_price', $clientMembership?->applied_price) }}" required class="mt-1 block w-full rounded-lg border-slate-300"><x-field-error name="applied_price" /></div><div><label for="status" class="text-sm font-medium text-slate-700">Estado</label><select id="status" name="status" class="mt-1 block w-full rounded-lg border-slate-300">@foreach ($statuses as $status)<option value="{{ $status->value }}" @selected(old('status', $clientMembership?->status->value ?? 'pending') === $status->value)>{{ ucfirst($status->value) }}</option>@endforeach</select><x-field-error name="status" /></div><div class="sm:col-span-2"><label for="observations" class="text-sm font-medium text-slate-700">Observaciones</label><textarea id="observations" name="observations" rows="4" class="mt-1 block w-full rounded-lg border-slate-300">{{ old('observations', $clientMembership?->observations) }}</textarea><x-field-error name="observations" /></div></div><div class="mt-7 flex flex-wrap gap-3"><button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">{{ $clientMembership ? 'Guardar cambios' : 'Asignar membresía' }}</button><a href="{{ $clientMembership ? route('client-memberships.show', $clientMembership) : route('client-memberships.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50">Cancelar</a></div></form>
@endsection
