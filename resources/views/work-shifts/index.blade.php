@extends('layouts.admin')

@section('title', 'Turnos')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Personal</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Turnos</h1><p class="mt-2 text-slate-600">Configura horarios laborales y asignaciones vigentes.</p></div><div class="flex flex-wrap gap-2"><a href="{{ route('employee-shift-assignments.index') }}" class="rounded-lg border border-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-indigo-700 hover:bg-indigo-50">Ver asignaciones</a><a href="{{ route('work-shifts.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nuevo turno</a></div></div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Turno</th><th class="px-5 py-3">Horario</th><th class="px-5 py-3">Asignaciones</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($workShifts as $workShift)<tr><td class="px-5 py-4 font-semibold text-slate-800">{{ $workShift->name }}</td><td class="px-5 py-4 text-slate-600">{{ substr($workShift->start_time, 0, 5) }} - {{ substr($workShift->end_time, 0, 5) }}{{ $workShift->spansMidnight() ? ' (dia siguiente)' : '' }}</td><td class="px-5 py-4 text-slate-600">{{ $workShift->employee_shift_assignments_count }}</td><td class="px-5 py-4"><x-status-badge :active="$workShift->is_active" /></td><td class="px-5 py-4 text-right"><div class="inline-flex items-center gap-3"><a href="{{ route('work-shifts.edit', $workShift) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Editar</a><form method="POST" action="{{ route('work-shifts.toggle-status', $workShift) }}">@csrf @method('PATCH')<button class="text-sm font-medium text-slate-700 hover:text-slate-500">{{ $workShift->is_active ? 'Desactivar' : 'Activar' }}</button></form></div></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No hay turnos configurados.</td></tr>@endforelse</tbody></table></div>@if ($workShifts->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $workShifts->links() }}</div>@endif</div>
@endsection
