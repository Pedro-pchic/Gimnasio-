@extends('layouts.admin')

@section('title', 'Membresías de clientes')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Operación</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Membresías de clientes</h1><p class="mt-2 text-slate-600">Asigna, consulta y actualiza membresías sin registrar cobros.</p></div>@can('client-memberships.manage')<a href="{{ route('client-memberships.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Asignar membresía</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Cliente</th><th class="px-5 py-3">Tipo</th><th class="px-5 py-3">Vigencia</th><th class="px-5 py-3">Precio aplicado</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($clientMemberships as $clientMembership)<tr><td class="px-5 py-4"><a href="{{ route('clients.show', $clientMembership->client) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $clientMembership->client->first_name }} {{ $clientMembership->client->last_name }}</a></td><td class="px-5 py-4 text-slate-600">{{ $clientMembership->membershipType->name }}</td><td class="px-5 py-4 text-slate-600">{{ $clientMembership->start_date->format('d/m/Y') }} – {{ $clientMembership->end_date->format('d/m/Y') }}</td><td class="px-5 py-4 text-slate-600">{{ number_format((float) $clientMembership->applied_price, 2) }}</td><td class="px-5 py-4"><x-status-badge :active="$clientMembership->status === \App\ClientMembershipStatus::Active" :active-label="$clientMembership->status->value" :inactive-label="$clientMembership->status->value" /></td><td class="px-5 py-4 text-right"><a href="{{ route('client-memberships.show', $clientMembership) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No hay membresías asignadas.</td></tr>@endforelse</tbody></table></div>@if ($clientMemberships->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $clientMemberships->links() }}</div>@endif</div>
@endsection
