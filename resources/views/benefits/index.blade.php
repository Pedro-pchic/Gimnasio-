@extends('layouts.admin')

@section('title', 'Beneficios')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Catálogo</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Beneficios</h1><p class="mt-2 text-slate-600">Configura los beneficios que pueden otorgarse en los planes.</p></div>@can('benefits.manage')<a href="{{ route('benefits.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nuevo beneficio</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Beneficio</th><th class="px-5 py-3">Tipo y valor</th><th class="px-5 py-3">Membresías</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($benefits as $benefit)<tr><td class="px-5 py-4"><a href="{{ route('benefits.show', $benefit) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $benefit->name }}</a><p class="mt-1 text-xs text-slate-500">{{ $benefit->description ?: 'Sin descripción' }}</p></td><td class="px-5 py-4 text-slate-600">{{ $benefit->benefit_type ?: 'No definido' }}@if ($benefit->value !== null)<br>{{ number_format((float) $benefit->value, 2) }}@endif</td><td class="px-5 py-4 text-slate-600">{{ $benefit->membershipTypes->pluck('name')->join(', ') ?: 'Sin asignar' }}</td><td class="px-5 py-4"><x-status-badge :active="$benefit->is_active" /></td><td class="px-5 py-4 text-right"><a href="{{ route('benefits.show', $benefit) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No hay beneficios registrados.</td></tr>@endforelse</tbody></table></div>@if ($benefits->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $benefits->links() }}</div>@endif</div>
@endsection
