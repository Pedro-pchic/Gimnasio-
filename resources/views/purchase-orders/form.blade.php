@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-3xl space-y-6"><h1 class="text-2xl font-semibold">Nueva orden de compra</h1><form method="POST" action="{{ route('purchase-orders.store') }}" class="space-y-4 rounded-lg bg-white p-6 shadow ring-1 ring-gray-200">@csrf<x-field-error field="supplier_id" /><select name="supplier_id" required class="w-full rounded border-gray-300"><option value="">Proveedor</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id }}" @selected(old('supplier_id') == $supplier->id)>{{ $supplier->name }}</option>@endforeach</select><select name="branch_id" required class="w-full rounded border-gray-300"><option value="">Sucursal</option>@foreach($branches as $branch)<option value="{{ $branch->id }}">{{ $branch->name }}</option>@endforeach</select><input type="date" name="order_date" value="{{ old('order_date', now()->toDateString()) }}" required class="w-full rounded border-gray-300"><select name="items[0][inventory_item_id]" required class="w-full rounded border-gray-300"><option value="">Artículo</option>@foreach($inventoryItems as $item)<option value="{{ $item->id }}">{{ $item->name }} ({{ $item->unit }})</option>@endforeach</select><div class="grid grid-cols-2 gap-4"><input type="number" min="1" name="items[0][quantity]" value="1" required placeholder="Cantidad" class="rounded border-gray-300"><input type="number" min="0" step="0.01" name="items[0][unit_cost]" required placeholder="Costo unitario" class="rounded border-gray-300"></div><textarea name="notes" placeholder="Notas" class="w-full rounded border-gray-300">{{ old('notes') }}</textarea><button class="rounded bg-indigo-600 px-4 py-2 text-white">Crear orden</button></form></div>
@endsection
