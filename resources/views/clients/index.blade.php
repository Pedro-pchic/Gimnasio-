@extends('layouts.admin')

@section('title', 'Clientes')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Personas</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Clientes</h1><p class="mt-2 text-slate-600">Consulta y actualiza la información de los clientes registrados.</p></div>@can('clients.manage')<a href="{{ route('clients.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Nuevo cliente</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Código</th><th class="px-5 py-3">Cliente</th><th class="px-5 py-3">Sucursal</th><th class="px-5 py-3">Contacto</th><th class="px-5 py-3">Estado</th><th class="px-5 py-3"><span class="sr-only">Acciones</span></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($clients as $client)<tr><td class="px-5 py-4 font-mono text-xs text-slate-600">{{ $client->code }}</td><td class="px-5 py-4"><a href="{{ route('clients.show', $client) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $client->first_name }} {{ $client->last_name }}</a><p class="mt-1 text-xs text-slate-500">Inscrito: {{ $client->registration_date->format('d/m/Y') }}</p></td><td class="px-5 py-4 text-slate-600">{{ $client->branch->name }}</td><td class="px-5 py-4 text-slate-600">{{ $client->phone ?: $client->email ?: 'Sin contacto' }}</td><td class="px-5 py-4"><x-status-badge :active="$client->is_active" /></td><td class="px-5 py-4 text-right"><a href="{{ route('clients.show', $client) }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">Ver</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No hay clientes registrados.</td></tr>@endforelse</tbody></table></div>@if ($clients->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $clients->links() }}</div>@endif</div>
@endsection
