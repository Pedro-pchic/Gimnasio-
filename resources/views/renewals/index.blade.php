@extends('layouts.admin')

@section('title', 'Renovaciones')

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">Finanzas</p><h1 class="mt-1 text-3xl font-bold tracking-tight">Renovaciones</h1><p class="mt-2 text-slate-600">Historial de membresías creadas junto con su pago.</p></div>@can('renewals.manage')<a href="{{ route('renewals.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">Registrar renovación</a>@endcan</div>
    <div class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Cliente</th><th class="px-5 py-3">Membresía</th><th class="px-5 py-3">Vigencia</th><th class="px-5 py-3">Pago</th><th class="px-5 py-3">Estado</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($renewals as $renewal)<tr><td class="px-5 py-4"><a href="{{ route('clients.show', $renewal->client) }}" class="font-semibold text-indigo-700 hover:text-indigo-500">{{ $renewal->client->first_name }} {{ $renewal->client->last_name }}</a></td><td class="px-5 py-4 text-slate-600">{{ $renewal->membershipType->name }}</td><td class="px-5 py-4 text-slate-600">{{ $renewal->start_date->format('d/m/Y') }} – {{ $renewal->end_date->format('d/m/Y') }}</td><td class="px-5 py-4 text-slate-600">@if ($renewal->payments->first())<a href="{{ route('payments.show', $renewal->payments->first()) }}" class="font-medium text-indigo-700 hover:text-indigo-500">{{ number_format((float) $renewal->payments->first()->amount, 2) }}</a>@else — @endif</td><td class="px-5 py-4"><x-status-badge :active="$renewal->status === \App\ClientMembershipStatus::Active" :active-label="$renewal->status->value" :inactive-label="$renewal->status->value" /></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">No hay renovaciones registradas.</td></tr>@endforelse</tbody></table></div>@if ($renewals->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $renewals->links() }}</div>@endif</div>
@endsection
