@extends('layouts.admin')

@section('title', 'Venta #'.$sale->id)

@section('content')
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><a href="{{ route('sales.index') }}" class="text-sm font-medium text-indigo-700 hover:text-indigo-500">← Volver a ventas</a><h1 class="mt-3 text-3xl font-bold tracking-tight">Venta #{{ $sale->id }}</h1><p class="mt-2 text-slate-600">{{ $sale->sale_date->format('d/m/Y') }} · {{ $sale->branch->name }} · {{ $sale->user->name }}</p></div><div class="flex flex-wrap gap-2">@if ($sale->receipt)<a href="{{ route('sales.receipt', $sale) }}" class="rounded-lg border border-indigo-600 px-4 py-2.5 text-sm font-semibold text-indigo-700 hover:bg-indigo-50">Ver comprobante</a>@endif @can('sales.manage')@if ($sale->status !== \App\SaleStatus::Cancelled)<form method="POST" action="{{ route('sales.cancel', $sale) }}" onsubmit="return confirm('¿Deseas cancelar esta venta?');">@csrf @method('PATCH')<button class="rounded-lg border border-red-300 px-4 py-2.5 text-sm font-semibold text-red-700 hover:bg-red-50">Cancelar venta</button></form>@endif @endcan</div></div>
    <div class="grid gap-5 lg:grid-cols-[2fr_1fr]"><section class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><div class="border-b border-slate-200 px-5 py-4"><h2 class="text-lg font-bold">Detalle</h2></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-3">Concepto</th><th class="px-5 py-3">Tipo</th><th class="px-5 py-3">Cantidad</th><th class="px-5 py-3">Precio</th><th class="px-5 py-3">Subtotal</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($sale->details as $detail)<tr><td class="px-5 py-4 text-slate-800">{{ $detail->description }}</td><td class="px-5 py-4 text-slate-600">{{ ucfirst($detail->concept_type->value) }}</td><td class="px-5 py-4 text-slate-600">{{ number_format((float) $detail->quantity, 2) }}</td><td class="px-5 py-4 text-slate-600">{{ number_format((float) $detail->unit_price, 2) }}</td><td class="px-5 py-4 font-medium text-slate-800">{{ number_format((float) $detail->subtotal, 2) }}</td></tr>@endforeach</tbody></table></div></section><aside class="rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><h2 class="text-lg font-bold">Resumen</h2><dl class="mt-5 space-y-4"><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Cliente</dt><dd class="mt-1 text-slate-800">{{ $sale->client ? $sale->client->first_name.' '.$sale->client->last_name : 'Sin cliente' }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Estado</dt><dd class="mt-1 text-slate-800">{{ ucfirst($sale->status->value) }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Subtotal</dt><dd class="mt-1 text-slate-800">{{ number_format((float) $sale->subtotal, 2) }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Descuento</dt><dd class="mt-1 text-slate-800">{{ number_format((float) $sale->discount, 2) }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Total</dt><dd class="mt-1 text-2xl font-bold text-slate-900">{{ number_format((float) $sale->total, 2) }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Pago</dt><dd class="mt-1 text-slate-800">@if ($sale->payments->first())<a href="{{ route('payments.show', $sale->payments->first()) }}" class="font-medium text-indigo-700 hover:text-indigo-500">{{ ucfirst($sale->payments->first()->payment_method->value) }}</a>@else Sin pago @endif</dd></div></dl></aside></div>
@endsection
